# 배당 모아 해외여행

## 0. 기본 정보
- 이름 : 배당 모아 해외여행
- 배포 주소 : https://dividend-travel-planner.vercel.app/
- 사용한 AI 모델/API : Gemini 2.5 Flash (Google Generative Language API, REST 직접 호출) + 참고용으로 Yahoo Finance 시세 API(현재가 스냅샷, AI 아님)

## 1. 한 줄 소개
매달 여행비만큼의 배당금을 받고 싶은 투자자에게, 배당킹·배당귀족 종목과 같이 이미 검증된 배당주 중에서 1월부터 12월까지 빠짐없이 배당이 들어오는 조합을 AI가 짜주는 배당 포트폴리오 플래너.

## 2. 이런 사람을 위해 만들었습니다
- 대상 : 10년, 20년 장기 투자를 통해 "매달 들어오는 여행비" 정도의 현금을 목표로 배당투자를 막 시작하려는 초보 투자자
- 그 사람이 지금 겪는 불편 : 어떤 배당주를 사야 매달 배당이 들어오는지, 그 조합으로 목표 금액까지 몇 년이 걸리는지를 스스로 계산하기 어려움
- 이 서비스를 쓰면 달라지는 것 : 목표 월배당금액과 매달 투자 가능한 금액만 입력하면, AI가 짜준 포트폴리오 후보 2개와 30년치 배당성장 시나리오, 월별 배당지급 캘린더를 바로 받아볼 수 있음

## 3. 주요 기능 (3개 이내)
1. **AI 포트폴리오 생성** — 목표 월배당금액 + 월 투자계획금액(+ 선택 입력으로 "담배·에너지주는 빼줘" 같은 선호/제외 조건)을 넣으면, Gemini가 배당킹/귀족주 큐레이션 리스트 안에서 12개월 지급월 커버리지 + 섹터 편중 방지(동일 섹터 40% 이하)를 만족하는 배분안 2개(서로 다른 트레이드오프)를 제안한다. 각 종목의 비중에는 실제 배당수익률·연속증가연수를 인용한 근거 문장이 따라붙는다.
2. **배당 성장 시뮬레이터** — AI는 배분과 조언만 하고, 연차별 배당금(1·5·10·15·20·25·30년차, 배당성장 vs 배당성장+재투자)은 서버 코드가 결정론적으로 계산해서 표와 차트로 보여준다. 목표 도달 예상 연차도 함께 표시된다.
3. **리스크 스크리닝 & 플랜 관리** — 후보에 포함된 종목들을 대상으로 Gemini의 웹검색(그라운딩)을 돌려 최근 6개월 내 배당 삭감/인상 뉴스를 배지로 보여준다(배분 비중은 자동으로 안 바뀌고, "이 종목 빼고 다시 만들기"로 사용자가 직접 조정). 선택한 조합은 Supabase에 저장되고 파이차트·종목별 상세·월별 배당지급 캘린더로 확인, 이름 변경/삭제/재생성도 가능하다.

## 4. AI를 어디에 어떻게 썼나요
서로 다른 두 가지 방식으로 Gemini를 쓴다.

**① 포트폴리오 배분 (스키마 강제 생성)**
- AI가 하는 일 : 종목별 배분(티커/비중), 비중마다 실제 수익률·연속증가연수를 인용한 근거 한 줄, 후보 컨셉 한 줄, 전략 조언 텍스트. 연차별 배당금 계산이나 목표 도달 시점 판단은 AI에게 맡기지 않고 서버 코드가 확정값으로 처리한다.
- 입력으로 무엇을 주고, 무엇을 받나요 : 목표 월배당금액, 월 투자계획금액, 선택 입력한 선호/제외 조건, 그리고 Supabase `dividend_stocks`에 저장된 배당주 리스트(티커/수익률/연속증가연수/섹터/배당지급월)를 JSON으로 프롬프트에 넣는다. `responseSchema`로 JSON 응답을 강제해서 후보 2개(배분표+근거+컨셉+조언)를 받는다.
- AI가 이상한 답을 줬을 때 어떻게 처리했나요 : 서버에서 네 가지를 검증한다 — (1) 리스트에 없는 티커(환각) (2) 비중 근거에 그 종목의 실제 수익률/연속증가연수 숫자가 인용되지 않음(근거 없는 지어낸 설명) (3) 비중 합계가 100이 아님 (4) 배당이 안 들어오는 달이 있음. 하나라도 걸리면 파싱 실패와 동일하게 처리해서 에러 메시지 + "다시 시도" 버튼을 보여주고 입력값은 그대로 유지한다. 목표 자체가 이 종목 리스트로는 물리적으로 불가능한 경우엔 AI를 호출하기 전에 서버가 먼저 걸러낸다(아래 8번 참고).
- 프롬프트에서 신경 쓴 점 1가지 : "매달 배당이 들어오는 것"이 이 서비스의 핵심 가치라서, 각 종목의 실제 배당지급월(payout_months)을 그대로 프롬프트에 주고 "후보 하나에 포함된 종목들의 지급월을 다 합치면 1~12월이 빠짐없이 커버돼야 한다"는 규칙을 못박았다. 처음엔 지급월을 A/B/C 3개 그룹으로 단순화해서 넣으려 했는데, 그룹으로 뭉치면 그룹 안에서 겹치는 달이 생겨 오히려 부정확해서 원본 지급월 배열을 그대로 쓰는 쪽으로 바꿨다.

**② 리스크 스크리닝 (웹검색 그라운딩, 스키마 강제 없음)**
- AI가 하는 일 : 후보에 포함된 종목마다 최근 6개월 내 배당 삭감/인상처럼 정적 DB에 없는 최신 뉴스를 웹검색으로 확인. 배분에는 반영하지 않고 참고 배지로만 보여준다(자동으로 종목을 빼지 않음 — 왜 그런지는 7번 참고).
- 입력/출력 : 티커 하나씩 그라운딩 호출(`tools: [{google_search:{}}]`)을 병렬로 보내고, 첫 단어로 `CUT`/`INCREASE`/`NONE`을 선언한 뒤 근거를 붙이는 자유 텍스트로 받는다. 출처 URL은 답변 텍스트가 아니라 API가 실제로 인용한 `groundingMetadata`에서 가져오고, 인용이 없으면 근거 없는 주장으로 보고 버린다.
- AI가 이상한 답을 줬을 때 어떻게 처리했나요 : 그라운딩 호출과 `responseSchema`(JSON 강제)는 같은 요청에서 같이 쓸 수 없다는 걸 실제 호출로 확인해서(400 에러), 스키마 강제 없이 자유 텍스트로 받고 서버 코드가 파싱한다. 실패/타임아웃되는 티커는 그 종목만 조용히 빼고 넘어간다(fail-open) — 리스크 정보는 부가 정보라 이것 때문에 플랜 생성 자체가 막히면 안 된다.

## 5. 데이터 설계
- 테이블 이름과 역할 :
  - `dividend_stocks` : 큐레이션한 배당킹/배당귀족 종목의 마스터 데이터. AI 프롬프트의 재료이자 `/stocks` 페이지에 그대로 노출된다.
  - `plans` : 사용자가 후보 2개 중 "선택"한 조합만 저장한다. 선택 안 한 후보는 저장하지 않는다.
- 주요 컬럼 :
  - `dividend_stocks` : ticker, name, sector, dividend_yield, dividend_growth_5y, consecutive_years, payout_months(지급월 배열), business_summary
  - `plans` : anonymous_id / user_id, target_monthly_dividend, monthly_investment, allocations(jsonb, 생성 시점 주가 스냅샷 포함), chart_data(jsonb, 연차별 계산 결과), advice_text, goal_achieved, name
- 이렇게 설계한 이유 : allocations/chart_data를 jsonb로 통째로 저장해두면 나중에 종목 정보나 계산 로직이 바뀌어도 이미 저장된 플랜은 저장 당시 값 그대로 보여줄 수 있다(스냅샷). 로그인 없이도 쓸 수 있어야 해서 브라우저에 저장하는 anonymous_id를 기본 식별자로 두고, 이후 로그인 기능을 붙이면서 user_id 컬럼을 추가해 로그인 사용자는 계정 기준, 비로그인 사용자는 anonymous_id 기준으로 자기 플랜만 조회하게 했다.

## 6. 화면 흐름
1. 로그인/회원가입(이메일+비밀번호, Supabase Auth) 후 홈에서 "배당주 투자가 뭔지 자세히 보기(`/about`)" 또는 "바로 플랜 짜기(`/plan`)" 중 선택한다.
2. `/plan`에서 목표 월배당금액과 월 투자계획금액, 선택으로 선호/제외 조건을 입력하고 "플랜 만들기"를 누르면, 같은 화면에 AI가 만든 후보 2개(배분표 + 근거 + 30년 시나리오 차트 + 조언 텍스트)가 나타난다. 종목에 최근 배당 삭감/인상 뉴스가 있으면 배지로 표시되고, 삭감 쪽 배지에는 "이 종목 빼고 다시 만들기" 버튼이 붙어 눌러서 바로 재생성할 수 있다.
3. 카드 하나를 "이 플랜 선택하기"로 고르면 그 조합만 Supabase에 저장되고 `/plans/[id]` 상세화면으로 이동한다. 상세화면은 탭으로 "플랜 상세(비중 파이차트+종목별 정보) / 배당 시뮬레이터(연차별 재투자 시나리오) / 배당지급일(월별 캘린더)"을 오가며 볼 수 있고, "입력값 수정하기"로 다시 생성하면 새 플랜으로 따로 저장된다.
4. `/plans`에서 지금까지 만든 플랜들을 카드로 모아보고, 이름을 바꾸거나 삭제할 수 있다.

## 7. 기획서와 달라진 점
- 뺀 것 1 : 희망 기간(년) 범위 입력. 기획 당시엔 "몇 년 안에 목표를 채우고 싶은지"를 min~max로 따로 받으려 했다.
- 뺀 이유 : 실제로 짜보니 월 투자금만 정해지면 도달 시점은 계산으로 나오는 값이라, 사용자가 기간을 직접 입력할 필요가 없었다. 대신 서버가 1~30년 전체를 계산해서 목표가 언제쯤 채워지는지 시나리오로 보여주는 쪽으로 바꿨다.
- 뺀 것 2 : 지급월 A/B/C 3분류. 기획서에서는 이해하기 쉽게 지급월을 세 그룹으로 단순화했었다.
- 뺀 이유 : 그룹으로 뭉치면 그룹 안에서 지급월이 겹쳐서 오히려 "매달 커버" 검증이 부정확해졌다. 종목별 실제 지급월 배열을 그대로 쓰는 게 더 정확해서 그쪽으로 정리했다.
- 시간이 더 있었다면 넣었을 것 : 실시간 시세 API를 계속 갱신해서 "지금 이 가격이면 몇 주를 살 수 있는지"까지 자동 계산해주는 기능. 지금은 플랜 생성 시점의 주가를 한 번 스냅샷으로 저장하는 정도까지만 했다.

## 8. 만들면서 가장 오래 막혔던 것
- 문제 : 웹디자인을 직접 해보고 싶었는데, 화면을 하나씩 만들다 보니 화면마다 색감·타이포그래피·여백 같은 디자인 톤이 제각각이 되는 게 오래 막혔다. 배당 시뮬레이터의 그래프 애니메이션도 원하는 만큼 자연스럽게 안 나왔다.
- 어떻게 해결했나 : 기능과 레이아웃 같은 토대는 직접 짜고, 그 토대를 [Stitch](https://stitch.withgoogle.com/)에 넣어서 화면 전체의 디자인 톤(컬러·타이포그래피·여백)을 하나의 디자인 시스템으로 뽑아낸 다음, 그 결과를 기준으로 다시 코드에 입혔다 — 톤 통일 문제는 이 방식으로 풀렸다. 그래프 애니메이션은 [21st.dev](https://21st.dev/)에서 원하던 느낌의 그래프 디자인을 찾아서 그 프롬프트를 그대로 가져와 적용했다.
