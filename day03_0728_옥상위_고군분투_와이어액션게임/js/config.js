// 손맛 튜닝 파라미터를 한 곳에 모아둔다.
// 단위: 거리 = 논리 픽셀(960x540 기준), 시간 = 초
//
// 개별 const가 아니라 하나의 객체로 내보내는 이유:
// ES 모듈의 const는 밖에서 수정할 수 없어서, 실행 중에 값을 바꿔가며 손맛을 확인할 수 없다.
// 객체로 두면 devtools.js의 튜닝 패널이 값을 바로 바꿔 즉시 체감해볼 수 있다.

export const TUNING = {
  /** 중력 가속도 (px/s²) */
  GRAVITY: 2200,

  /** 종단 속도 — 너무 빨라져서 충돌 판정을 뚫고 지나가는 것을 방지 (px/s) */
  MAX_FALL_SPEED: 900,

  /**
   * 지상 점프의 초기 상승 속도 (위쪽이 음수).
   * 최고 높이 = v²/(2g) = 650²/(2×2200) ≈ 96px, 체공 시간(2v/g) ≈ 0.59초.
   */
  JUMP_VELOCITY: -650,

  /** 와이어 사거리. 조준선 길이로도 그대로 표시된다. */
  WIRE_MAX_LENGTH: 300,

  /**
   * 탑의 와이어 부착 판정 반경. 크면 관대해지고 작으면 정밀 조준을 요구한다.
   * 조준이 너무 어렵다는 피드백을 받아, 가로대 폭을 3배(±27→±81)로 넓힌 것에 맞춰
   * 판정 반경도 3배(18→54)로 늘렸다.
   */
  ANCHOR_RADIUS: 54,

  /** 헛방 쳤을 때 붉은 선이 남는 시간 (초) */
  WIRE_MISS_FLASH: 0.2,

  /** 구간 퍼펙트 문구가 떠올랐다 사라지는 시간 (초) */
  PERFECT_FLASH_DURATION: 1.2,

  /**
   * 화면 아래로 이만큼 더 떨어지면 추락 확정 (px).
   * 화면 밖에서 판정하는 이유: 화면 경계에서 즉시 끊으면 "떨어졌다"는 느낌 없이
   * 갑자기 게임이 멈춘 것처럼 보인다. 떨어지는 모습을 잠깐 보여준 뒤 판정한다.
   */
  FALL_DEATH_MARGIN: 60,

  /**
   * 스윙에만 적용하는 중력 배율. **스윙 손맛의 핵심 손잡이.**
   *
   * 진자 주기는 2π√(L/g)라서, 이 값이 없으면 스윙 속도가 줄 길이에만 좌우된다.
   * 즉 탑을 어디 놓느냐에 따라 손맛이 딴 게임이 되어버린다.
   * 낙하 속도(GRAVITY)는 그대로 두고 스윙만 따로 조여줄 수 있게 분리했다.
   *
   * 1이면 물리 그대로(느긋함), 크면 빠르고 경쾌해지지만 튕겨나가는 속도도 함께 커진다.
   * (스윙 시간은 1/√배율, 최고 속도는 √배율로 변한다)
   */
  SWING_GRAVITY_SCALE: 1.8,

  /**
   * 스윙 감쇠 계수 (프레임당 각속도에 곱한다).
   * 1이면 감쇠 없음 — 스윙할수록 속도가 쌓여 계속 나아갈 수 있다.
   */
  SWING_DAMPING: 1,

  /**
   * 와이어를 놓는 순간 접선 속도에 곱하는 배율.
   * 1이면 물리 그대로, 1보다 크면 '탁' 튕겨 나가는 과장된 손맛이 된다.
   * 너무 크면 연속 스윙에서 속도가 눈덩이처럼 불어나니 주의.
   */
  RELEASE_BOOST: 1,

  /**
   * 오토런 속도 (px/s). 지상에서 항상 이 속도로 전진하며, 클릭 없이 지붕 끝에
   * 도달하면 그대로 걸어서 떨어진다(점프하지 않아도 낭떠러지 위로 나가게 된다).
   * 공중에서는 이 값이 직접 강제되지 않고, 이탈 시점의 속도가 그대로 관성으로 유지된다.
   */
  AUTO_RUN_SPEED: 180,

  /**
   * 스테이지가 하나 올라갈 때마다 붙는 진행 속도 증가폭.
   * 스테이지의 speedScale = 1 + (스테이지번호 - 1) × 이 값.
   * 0.12면 6단계에서 1.60배가 되어, 지붕 끝까지 걸리는 시간이 약 38% 줄어든다.
   *
   * 스윙 물리(SWING_GRAVITY_SCALE)에는 일부러 적용하지 않는다 —
   * 스윙 감각은 스테이지마다 같아야 플레이어가 익힌 타이밍을 계속 쓸 수 있고,
   * 난이도는 '판단할 시간이 줄어드는 것'으로만 올린다.
   */
  STAGE_SPEED_STEP: 0.12,

  /**
   * 개발용 속도 미리보기 배율 (배포 전 제거).
   * 스테이지를 넘기지 않고도 빠른 속도를 체감해보기 위한 임시 손잡이.
   * CP25 밸런스 조정 결과 1.5로 확정 — 코스 진행 배율(1.0~1.6배) 위에 추가로 곱해진다.
   */
  SPEED_SCALE_PREVIEW: 1.5,

  /**
   * 카메라가 플레이어를 따라가기 시작하는 화면 x 위치(px).
   * 플레이어가 이 위치를 넘어서기 전까지는 카메라가 고정되어 있고,
   * 넘어서면 이 위치에 플레이어가 계속 보이도록 화면이 스크롤된다.
   */
  CAMERA_FOLLOW_X: 250,
};
